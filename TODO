# Things still to do...

 * Build as a MSI package?
 * Consider adding explicit case-handling, e.g.
   * skeleton:u --uppercase (e.g. FOO_BAR)
   * skeleton:c --camel case (e.g. fooBar)
   * skeleton:C --upper camel case (e.g. FooBar)
   * skeleton:t --title case (e.g. Foo Bar)
   * skeleton:_ --snake case (e.g. foo_bar)
   * skeleton:- --kebab case (e.g. foo-bar)
